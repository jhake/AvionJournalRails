
<h2>
  <%= @category.name %>
</h2>
<p>
  <%= @category.description %>
</p>

<h3><%= link_to 'Show Tasks', category_tasks_path(@category.id)%></h3>

<%= link_to 'Edit', edit_category_path(@category.id) %>
<%= link_to 'Back to Categories', categories_path %>
